select_employees =          SELECT userid, email, rolename, firstname, lastname, statusname, phonenumber \
                            FROM ((users JOIN roles ON users.roleid= roles.roleid) \
                                            JOIN user_status ON users.statusid=user_status.statusid)

select_employee_by_id =     SELECT userid, email, rolename, firstname, lastname, statusname, phonenumber \
                            FROM ((users JOIN roles ON users.roleid = roles.roleid) \
                                            JOIN user_status ON users.statusid = user_status.statusid) \
                            WHERE userid = ?

insert_employee =           INSERT INTO users(email, rolename, firstname, lastname, statusname  , phonenumber) \
                            VALUES (?, ?, ?, ?, ?, ?)

update_employee =           UPDATE users \
                            SET email = ?, userrole = ?, firstname = ?, lastname = ?, status = ?, phonenumber = ? \
                            WHERE userid = ?

delete_employee =           DELETE FROM user \
                            WHERE userid = ?